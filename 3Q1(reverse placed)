#include <stdio.h>
#include <stdlib.h>
#include<math.h>

int main()
{
int n,rem,res,res1,count,t,m;
printf("enter the number\n");
scanf("%d",&n);
while(n!=0)
{
   rem=n%10;
  printf("%d",rem);
   res=n-rem;
   res1=res/10;
   n=res1;
   count++;

}
 printf("\ntotal number of digits:%d",count);

return;
   }
